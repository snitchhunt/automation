These are the final datasets that should be imported into ElasticSearch by the scripts in `scripts/ingestion`. 

They are a combination of the randomly generated and the curated datasets from `../data-work`.